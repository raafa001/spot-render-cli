[project]
name = "spotrender"
version = "0.1.0"
description = "CLI for Spot Render uploads"
requires-python = ">=3.12"
dependencies = [
  "click>=8.1",
  "requests>=2.34",
]

[project.scripts]
spotrender = "spotrender.client:main"
